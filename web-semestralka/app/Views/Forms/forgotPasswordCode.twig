<div class="container py-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h3 class="mb-3">Ověření kódu</h3>
                    <p class="small text-muted">
                        Na e-mail jsme odeslali ověřovací kód. Zadejte ho prosím níže.
                    </p>

                    <form method="post">
                        <div class="mb-3">
                            <label class="form-label">Ověřovací kód</label>
                            <input type="text" name="reset_code" class="form-control">
                        </div>

                        <div class="d-flex justify-content-between">
                            <button class="btn btn-outline-secondary"
                                    type="submit"
                                    name="forgotCancel">
                                Zavřít / zrušit
                            </button>

                            <button class="btn btn-primary"
                                    type="submit"
                                    name="forgotPasswordVerify">
                                Ověřit kód
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
